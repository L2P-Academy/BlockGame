//MS, DJ

package blockGame.controller;

/* This is an interface for anything that needs to be updated regularly (like every frame) */

public interface Updatable {
 void update(); // method to be called by the game loop
}
